<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/swipeRefreshLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragment.SellerProductsFragment">

    <!-- RecyclerView, 로딩바, 빈 상태 텍스트를 관리하는 컨테이너 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- 상품 목록을 보여줄 RecyclerView -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerProducts"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:visibility="gone"
            tools:listitem="@layout/item_product" /> <!-- 미리보기를 위한 아이템 레이아웃 (선택 사항) -->

        <!-- 데이터 로딩 중에 표시될 ProgressBar -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:visibility="visible" /> <!-- 처음에는 보이도록 설정 -->

        <!-- 상품이 없을 때 표시될 TextView -->
        <TextView
            android:id="@+id/tvEmptyState"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="등록된 상품이 없습니다."
            android:textSize="16sp"
            android:visibility="gone" /> <!-- 처음에는 숨겨진 상태 -->

    </RelativeLayout>

</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>